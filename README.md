# emacs-config
Emacs configs for my different devices

# Init config file for Emacs in normal mode (non-daemon-mode): init.el

# Init config folders for Emacs in daemon-mode: emacs-minimal.zip, emacs-power.zip
